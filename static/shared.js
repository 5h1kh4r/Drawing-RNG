function makeParticipantId(){let x=localStorage.getItem('drng_participant_id');if(!x){x='p_'+Math.random().toString(16).slice(2,10);localStorage.setItem('drng_participant_id',x)}return x}
function newParticipantId(){const x='p_'+Math.random().toString(16).slice(2,10);localStorage.setItem('drng_participant_id',x);return x}
function setupCanvas(canvas, countEl){const ctx=canvas.getContext('2d');let strokes=[], current=null;function bg(){ctx.fillStyle='#fbfdff';ctx.fillRect(0,0,canvas.width,canvas.height);ctx.strokeStyle='#e8eef6';ctx.lineWidth=1;for(let x=80;x<canvas.width;x+=80){ctx.beginPath();ctx.moveTo(x,0);ctx.lineTo(x,canvas.height);ctx.stroke()}for(let y=80;y<canvas.height;y+=80){ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(canvas.width,y);ctx.stroke()}}function drawStroke(s,color='#172033',w=6){if(!s||s.length<2)return;ctx.strokeStyle=color;ctx.lineWidth=w;ctx.lineCap='round';ctx.lineJoin='round';ctx.beginPath();ctx.moveTo(s[0][0],s[0][1]);for(let i=1;i<s.length;i++)ctx.lineTo(s[i][0],s[i][1]);ctx.stroke()}function redraw(){bg();strokes.forEach(s=>drawStroke(s));if(current)drawStroke(current,'#0284c7');if(countEl)countEl.textContent=strokes.length+' stroke'+(strokes.length===1?'':'s')}function point(e){const r=canvas.getBoundingClientRect();return[(e.clientX-r.left)*(canvas.width/r.width),(e.clientY-r.top)*(canvas.height/r.height)]}canvas.addEventListener('pointerdown',e=>{e.preventDefault();canvas.setPointerCapture(e.pointerId);current=[point(e)];canvas.parentElement.classList.add('drawing');redraw()});canvas.addEventListener('pointermove',e=>{if(!current)return;e.preventDefault();const p=point(e),q=current[current.length-1];if(Math.hypot(p[0]-q[0],p[1]-q[1])>=1.5){current.push(p);redraw()}});function end(e){if(!current)return;e.preventDefault();if(current.length>=2)strokes.push(current);current=null;canvas.parentElement.classList.remove('drawing');redraw()}canvas.addEventListener('pointerup',end);canvas.addEventListener('pointercancel',end);canvas.addEventListener('pointerleave',e=>{if(current)end(e)});redraw();return{get:()=>strokes, set:(v)=>{strokes=v||[];current=null;redraw()}, clear:()=>{strokes=[];current=null;redraw()}, undo:()=>{strokes.pop();redraw()}, redraw}}
async function postJson(url, body){const res=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const data=await res.json();if(!res.ok)throw new Error(data.error||'Request failed');return data}
