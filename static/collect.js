const canvas=document.getElementById('canvas');const c=setupCanvas(canvas,document.getElementById('count'));const pid=document.getElementById('pid');pid.textContent=makeParticipantId();const concept=document.getElementById('concept');const promptText=document.getElementById('promptText');concept.onchange=()=>promptText.textContent=concept.value;document.getElementById('newPid').onclick=()=>{pid.textContent=newParticipantId()};document.getElementById('undo').onclick=()=>c.undo();document.getElementById('clear').onclick=()=>{c.clear();status('Cleared.')};function status(t,cls=''){const el=document.getElementById('status');el.textContent=t;el.className='output '+cls}let latest=null;document.getElementById('tokenize').onclick=async()=>{try{latest=await postJson('/api/tokenize',{strokes:c.get(),profile:'balanced'});status('Token count: '+latest.tokens.length+'\n'+latest.serialized,'ok')}catch(e){status(e.message,'bad')}};document.getElementById('save').onclick=async()=>{if(!document.getElementById('consent').checked){status('Consent checkbox required.','bad');return}try{if(!latest){latest=await postJson('/api/tokenize',{strokes:c.get(),profile:'balanced'})}const body={participant_id:pid.textContent,concept:concept.value,redraw_id:parseInt(document.getElementById('redraw').value||'1',10),sample_name:concept.value+'_redraw_'+String(document.getElementById('redraw').value||'1').padStart(2,'0'),notes:document.getElementById('notes').value,strokes:c.get(),serialized:latest.serialized,profile:'balanced',canvas_size:[canvas.width,canvas.height],ui_version:'collector-clean-v1'};const res=await postJson('/api/save_prompt_sample',body);status('Saved: '+JSON.stringify(res),'ok');document.getElementById('redraw').value=parseInt(document.getElementById('redraw').value||'1',10)+1;c.clear();latest=null}catch(e){status(e.message,'bad')}};
