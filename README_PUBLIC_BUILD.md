# Public Build Notes

This folder is safe to publish as a demo-oriented repository. It is intentionally stripped of dev-console routes, dataset tools, generated reports, and service-role-key workflows.

The public app can run fully offline/local. Optional Supabase logging must use an anon key plus insert-only RLS policies.
