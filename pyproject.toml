[project]
name = "drawing-rng"
version = "0.4.0"
description = "Drawing-RNG: hand-drawn graphical seed research prototype"
requires-python = ">=3.10"

dependencies = [
  "flask",
  "gunicorn",
  "supabase",
  "pillow",
  "matplotlib",
]

[tool.setuptools.packages.find]
where = ["src"]
